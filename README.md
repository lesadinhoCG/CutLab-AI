# CutLab AI

AplicaÃ§Ã£o local para transformar vÃ­deos longos em cortes verticais prontos para Shorts, Reels e TikTok.

## ExecuÃ§Ã£o

1. Crie um ambiente Python 3.10+ e instale as dependÃªncias:

   ```powershell
   python -m pip install -r requirements.txt
   ```

2. Configure as chaves necessÃ¡rias no arquivo `.env` (nÃ£o versionado).

3. Inicie a aplicaÃ§Ã£o:

   ```powershell
   python server.py
   ```

Ou execute `abrir_cutlab.bat` no Windows.

## Testes

```powershell
python -m pytest -q
```

## LicenÃ§a

Consulte `LICENSE`, se incluÃ­do nesta distribuiÃ§Ã£o.
